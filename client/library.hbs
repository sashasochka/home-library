<head>
  <title>Home library</title>
</head>

<body>
  <div id="main-pane">
    {{> books}}
  </div>

  <div id="side-pane">

  </div>
</body>

<template name="books">
  <div id="items-view">
    {{#if loading}}
      Loading...
    {{else}}
      <table cellpadding="0" cellspacing="0" border="0" id="data-table">
        <thead>
          <tr>
            {{#each columns}}
              <th class="{{class}}" title="{{title}}">{{title}}</th>
            {{/each}}
          </tr>
        </thead>
        {{#each books}}
          <tr>
            {{#each this}}
              <td class="{{class}}" title="{{value}}">{{value}}</td>
            {{/each}}
          </tr>
        {{/each}}
        <tfoot>
          <tr>
            {{#each columns}}
              <th class="{{class}}" title="{{title}}">{{title}}</th>
            {{/each}}
          </tr>
        </tfoot>
      </table>
      {{> pagination}}
    {{/if}}
  </div>
</template>

<template name="pagination">
  <ul id="pagination">
    <li class="{{active_previous_class}}" id="previous_page"><a href="#">← Previous</a></li>
    {{#each page}}
      <li class="{{active_class}}"><a href="#" class="page_number">{{isActive}} {{page_number}}</a></li>
    {{/each}}
    <li class="{{active_next_class}}"><a href="#" id="next_page">Next →</a></li>
  </ul>
</template>
